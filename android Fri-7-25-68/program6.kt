//logical operators
fun main(args: Array<String>) {
    val isadult = true
    val hasPermission = false

    println(isadult && hasPermission)
    println(isadult || hasPermission)
    println(!isadult) 
}